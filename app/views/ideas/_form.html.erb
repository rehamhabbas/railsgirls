<%= bootstrap_form_for(@idea) do |f| %>
  <% if @idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

      <ul>
      <% @idea.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, :placeholder=> 'whats your name' %>
  </div>
  <div class="field">
    <%= f.text_area :description ,:placeholder=> 'write a discription'%>
  </div>
  <div class="field">

    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-danger' %>
  </div>
<% end %>
